import { PortalUnavailable } from "@/components/PortalUnavailable";

export default function TenantContractPage() {
  return <PortalUnavailable portal="Tenant" description="Tenant contract access is locked until tenant portal accounts are configured." />;
}
