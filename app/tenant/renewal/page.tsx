import { PortalUnavailable } from "@/components/PortalUnavailable";

export default function TenantRenewalPage() {
  return <PortalUnavailable portal="Tenant" description="Tenant renewal access is locked until tenant portal accounts are configured." />;
}
